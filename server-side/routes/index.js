import {usersRoutes} from './users'
import {groupsRoutes} from './groups'
import {productsRoutes} from './products'

const routes = Object.freeze({usersRoutes, groupsRoutes, productsRoutes});

export default routes
